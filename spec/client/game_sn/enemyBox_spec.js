var config = require('../../../client/game_sn/config');
var EnemyBox = require('../../../client/game_sn/box/enemy_box');

var enemy = new EnemyBox(1, 2, 1, config, {});

describe('EnemyBox', 
  function(){

    beforeEach(
      function(done){

        done();
    });
    
    afterEach(
      function(done){

        done();
    });
    
    describe('', 
      function(){        
        
        it('', 
          function(done){
                done();
        });
        
        it('',
          function(done){
            done();
        });
        
        it('',
          function(done){
            done();
        });
        
        it('',
          function(done){
            done();
        });  
    });
});